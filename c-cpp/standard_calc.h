/**
 * You don't need to look at this file :)
 */

#pragma once

#include "stdbool.h"

float bound_to_180(float angle);
bool is_angle_between(float first_angle, float middle_angle, float second_angle);
